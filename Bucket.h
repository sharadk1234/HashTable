#include <iostream>
#include <map>
#include <vector>
#include <bitset>

class Bucket{

    int size_bucket, local_depth;
    int * array;

    public:
        Bucket();
        Bucket(int local_depth, int size_bucket);
        
};
